<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Page Migration" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Page Migration">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-wttj-beige min-h-screen flex flex-col">
    <!-- Navigation -->
    <nav class="bg-wttj-black text-white sticky top-0 z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <!-- Logo -->
          <div class="flex items-center">
            <%= link_to root_path, class: "flex items-center gap-2" do %>
              <span class="text-wttj-yellow font-bold text-xl">Page Migration</span>
            <% end %>
          </div>

          <!-- Navigation Links -->
          <div class="flex items-center gap-6">
            <%= link_to "Dashboard", root_path, class: "hover:text-wttj-yellow transition-colors #{request.path == root_path ? 'text-wttj-yellow' : ''}" %>
            <%= link_to "Search", organizations_path, class: "hover:text-wttj-yellow transition-colors #{request.path.start_with?('/organizations') ? 'text-wttj-yellow' : ''}" %>
            <%= link_to "Stats", stats_path, class: "hover:text-wttj-yellow transition-colors #{request.path.start_with?('/stats') ? 'text-wttj-yellow' : ''}" %>
            <%= link_to "Commands", commands_path, class: "hover:text-wttj-yellow transition-colors #{request.path.start_with?('/commands') ? 'text-wttj-yellow' : ''}" %>
            <%= link_to "Exports", exports_path, class: "hover:text-wttj-yellow transition-colors #{request.path.start_with?('/exports') ? 'text-wttj-yellow' : ''}" %>
            <%= link_to "Compare", comparisons_path, class: "hover:text-wttj-yellow transition-colors #{request.path.start_with?('/comparisons') ? 'text-wttj-yellow' : ''}" %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if flash[:notice] %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 mx-4 mt-4 rounded" role="alert">
        <%= flash[:notice] %>
      </div>
    <% end %>
    <% if flash[:alert] %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 mx-4 mt-4 rounded" role="alert">
        <%= flash[:alert] %>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 flex-grow w-full">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-wttj-gray-darker text-wttj-gray-medium py-4 mt-auto">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center text-sm">
        Page Migration CLI Web Interface
      </div>
    </footer>
  </body>
</html>
